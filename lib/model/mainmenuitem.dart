import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class MenuItem {
  String? title;
  IconData? icon;
  Widget? page;
  Color? color;

  MenuItem({this.title, this.icon, this.page, this.color});
}
